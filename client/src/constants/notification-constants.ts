import { offset } from '../../../shared/types/common';

export const DEF_NOTIFICATION_DURATION: number = 3000;
export const DEF_NOTIFICATION_OUT_TIME: number = 500;

export const LOW_NOTIFICATION: offset = { x: 0, y: -25 };
